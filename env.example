# Environment variables declared in this file are available at build time and runtime.
# Next.js will load environment variables from .env.local into process.env

# Database (PostgreSQL en production)
DATABASE_URL="postgresql://user:password@localhost:5432/dbname"

# NextAuth - CRITIQUE: Générer avec: openssl rand -base64 32
NEXTAUTH_SECRET="CHANGEZ_CETTE_VALEUR_EN_PRODUCTION"
NEXTAUTH_URL="http://localhost:3000"

# Google OAuth (optionnel)
GOOGLE_CLIENT_ID="votre_client_id"
GOOGLE_CLIENT_SECRET="votre_client_secret"

# OpenAI (pour éditeur JSON uniquement)
OPENAI_API_KEY="sk-..."

# Rate Limiting (optionnel - Upstash Redis)
UPSTASH_REDIS_REST_URL=""
UPSTASH_REDIS_REST_TOKEN=""
